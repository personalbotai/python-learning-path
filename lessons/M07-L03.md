# Encapsulation & Properties

**ID**: `M07-L03`  
**Type**: lesson  
**Duration**: 30 menit  
**Tags**: oop, encapsulation, properties, data-hiding

---

## Tujuan Pembelajaran

Setelah mengerjakan pelajaran ini, kamu akan mampu:

- memahami konsep encapsulation (enkapsulasi)
- menggunakan naming conventions untuk visibility (_protected, __private)
- membuat getter dan setter dengan `@property`
- melakukan validasi pada setter
- membuat computed properties
- memahami difference antara public, protected, dan private attributes

---

## Materi

### Konsep Dasar

**Encapsulation** adalah principle membundle data (attributes) dan methods yang bekerja pada data tersebut, serta membatasi akses langsung ke data internal. Tujuan: protect object's integrity, hide implementation details.

Python tidak memiliki strict private variables seperti Java/C++. Sebagai gantinya, Python menggunakan **naming conventions** dan **name mangling** untuk "soft" encapsulation.

**Visibility levels (convention):**
- `public`: no underscore (e.g., `name`) - bisa diakses dari mana saja
- `protected`: single underscore (e.g., `_balance`) - convention: internal use, bisa masih diakses
- `private`: double underscore (e.g., `__secret`) - name mangling, harder to access accidentally

### Detail Teknis

**1. Naming Conventions**

```python
class BankAccount:
    def __init__(self, owner, initial_balance):
        self.owner = owner           # public
        self._balance = initial_balance  # protected (convention)
        self.__pin = "1234"          # private (name mangling)
    
    def get_balance(self):
        """Public method to access protected attribute"""
        return self._balance
    
    def _internal_method(self):
        """Protected method - for internal use only"""
        pass
    
    def __private_method(self):
        """Private method - name mangling makes it harder to access"""
        pass

acc = BankAccount("Alice", 1000)
print(acc.owner)          # OK - public
print(acc.get_balance()) # OK - via getter
print(acc._balance)      # Possible but discouraged (protected)
# print(acc.__pin)       # AttributeError! Name mangling: _BankAccount__pin
print(acc._BankAccount__pin)  # Accessible but ugly (private)
```

**2. Property Decorators (Getters/Setters)**

```python
class Temperature:
    def __init__(self, celsius):
        self._celsius = celsius  # "private" attribute
    
    @property
    def celsius(self):
        """Getter - akses seperti attribute, bukan method"""
        return self._celsius
    
    @celsius.setter
    def celsius(self, value):
        """Setter - validasi sebelum set"""
        if not isinstance(value, (int, float)):
            raise TypeError("Temperature must be numeric")
        if value < -273.15:
            raise ValueError("Below absolute zero!")
        self._celsius = value
    
    @property
    def fahrenheit(self):
        """Computed property (read-only)"""
        return self._celsius * 9/5 + 32
    
    @property
    def kelvin(self):
        return self._celsius + 273.15

temp = Temperature(25)
print(temp.celsius)      # 25 (calls getter)
print(temp.fahrenheit)   # 77.0 (computed)
temp.celsius = 30        # calls setter
# temp.celsius = -300    # ValueError: Below absolute zero!
# temp.fahrenheit = 100  # AttributeError: can't set attribute (read-only)
```

**3. Full Property Pattern (getter, setter, deleter)**

```python
class Person:
    def __init__(self, name, age):
        self._name = name
        self._age = age
    
    @property
    def name(self):
        return self._name
    
    @name.setter
    def name(self, value):
        if not value.strip():
            raise ValueError("Name cannot be empty")
        self._name = value.strip()
    
    @name.deleter
    def name(self):
        print("Deleting name...")
        self._name = None
    
    @property
    def age(self):
        return self._age
    
    @age.setter
    def age(self, value):
        if not isinstance(value, int):
            raise TypeError("Age must be integer")
        if value < 0 or value > 150:
            raise ValueError("Invalid age")
        self._age = value

p = Person("Alice", 25)
print(p.name)  # Alice
p.name = "Bob"
del p.name     # calls deleter
```

**4. Read-Only Properties**

```python
class Circle:
    def __init__(self, radius):
        self._radius = radius
    
    @property
    def radius(self):
        return self._radius
    
    @property
    def area(self):
        import math
        return math.pi * self._radius ** 2
    
    @property
    def circumference(self):
        import math
        return 2 * math.pi * self._radius

c = Circle(5)
print(c.radius)      # 5
print(c.area)        # 78.539...
# c.radius = 10     # AttributeError: read-only (no setter)
# c.area = 100      # AttributeError: read-only
```

**5. Cached Properties (memoization)**

```python
from functools import cached_property

class DataLoader:
    def __init__(self, filepath):
        self.filepath = filepath
    
    @cached_property
    def data(self):
        """Expensive operation - cached after first call"""
        print(f"Loading data from {self.filepath}...")
        # Simulate expensive I/O
        import time
        time.sleep(2)
        return [1, 2, 3, 4, 5]  # dummy data

loader = DataLoader("big_file.txt")
print(loader.data)  # "Loading..." message, then data
print(loader.data)  # No loading, cached result
```

**6. Property vs Direct Attribute Access**

```python
# Without properties (direct access)
class Rectangle:
    def __init__(self, width, height):
        self.width = width  # public, no validation
        self.height = height

rect = Rectangle(4, 5)
rect.width = -10  # Oops, invalid but allowed!

# With properties (controlled access)
class Rectangle:
    def __init__(self, width, height):
        self._width = width
        self._height = height
    
    @property
    def width(self):
        return self._width
    
    @width.setter
    def width(self, value):
        if value <= 0:
            raise ValueError("Width must be positive")
        self._width = value

rect = Rectangle(4, 5)
# rect.width = -10  # ValueError!
```

**7. Composition vs Inheritance (Encapsulation perspective)**

```python
# Composition: "has-a" relationship (better encapsulation)
class Engine:
    def start(self):
        return "Engine started"

class Car:
    def __init__(self):
        self._engine = Engine()  # Composition
    
    def start(self):
        return self._engine.start()  # Delegation

# Inheritance: "is-a" relationship (tighter coupling)
class ElectricCar(Car):
    # Extends Car, but inherits all implementation details
    pass

# Prefer composition when you want to hide implementation details
```

---

## Latihan

### Soal 1: Temperature Class dengan Validation

Buat class `Temperature` yang:
- Store temperature dalam Celsius (private attribute `_celsius`)
- Provide properties:
  - `celsius` (getter/setter dengan validation -273.15 ≤ C ≤ 1000)
  - `fahrenheit` (read-only computed)
  - `kelvin` (read-only computed)
- Provide methods:
  - `to_fahrenheit()` - return Fahrenheit
  - `to_kelvin()` - return Kelvin
  - `from_fahrenheit(f)` - class method buat Temperature dari Fahrenheit
  - `from_kelvin(k)` - class method buat Temperature dari Kelvin

```python
class Temperature:
    # TODO: implement
    pass

def main():
    t = Temperature(25)
    print(f"{t.celsius}°C = {t.fahrenheit}°F = {t.kelvin}K")
    
    t.celsius = 100
    print(f"Boiling point: {t.fahrenheit}°F")
    
    # Class methods
    t2 = Temperature.from_fahrenheit(212)
    print(f"212°F = {t2.celsius}°C")
    
    # Validation test
    try:
        t.celsius = -300
    except ValueError as e:
        print(f"Error: {e}")
```

### Soal 2: Bank Account dengan Properties & Validation

Perluas `BankAccount` dari lesson sebelumnya dengan encapsulation:

```python
class BankAccount:
    def __init__(self, account_number, owner, initial_balance=0):
        # TODO: store sebagai "private" attributes
        # _account_number, _owner, _balance
        pass
    
    # Account number - read-only (tidak bisa diubah setelah init)
    @property
    def account_number(self):
        return self._account_number
    
    # Owner - getter/setter dengan validation (tidak kosong, min 3 chars)
    @property
    def owner(self):
        pass
    
    @owner.setter
    def owner(self, value):
        pass
    
    # Balance - read-only (hanya bisa diubah via deposit/withdraw)
    @property
    def balance(self):
        return self._balance
    
    # Methods: deposit, withdraw, transfer (dengan validation)
    def deposit(self, amount):
        # TODO: validate amount > 0
        pass
    
    def withdraw(self, amount):
        # TODO: validate amount > 0, amount <= balance
        pass
    
    def transfer(self, target_account, amount):
        # TODO: transfer ke account lain
        pass
    
    def __str__(self):
        return f"Account({self.account_number}, {self.owner}, Balance: ${self.balance:.2f})"

def main():
    acc1 = BankAccount("12345", "Alice", 1000)
    acc2 = BankAccount("67890", "Bob", 500)
    
    print(acc1)
    acc1.deposit(200)
    acc1.withdraw(100)
    acc1.transfer(acc2, 300)
    
    # Try invalid operations
    try:
        acc1.owner = ""  # invalid
    except ValueError as e:
        print(f"Validation error: {e}")
    
    try:
        acc1.withdraw(10000)  # insufficient
    except ValueError as e:
        print(f"Withdraw error: {e}")
```

### Soal 3: Lazy Loading dengan Cached Property

Buat class `ConfigLoader` yang:
- Menerima file path di `__init__`
- `config` property menggunakan `@cached_property` untuk load file hanya sekali
- File configuration adalah JSON
- Jika file tidak ada, return empty dict
- Provide `reload()` method untuk clear cache dan reload

```python
from functools import cached_property
import json
from pathlib import Path

class ConfigLoader:
    def __init__(self, config_path):
        self.config_path = Path(config_path)
    
    @cached_property
    def config(self):
        """Load JSON config, cached after first access"""
        # TODO:
        # 1. Cek file exists
        # 2. Jika ada, json.load(), return dict
        # 3. Jika tidak ada, return {}
        pass
    
    def reload(self):
        """Clear cached config dan reload dari file"""
        # HINT: hapus attribute dari __dict__ untuk cached_property
        if 'config' in self.__dict__:
            del self.__dict__['config']
        return self.config
    
    def get(self, key, default=None):
        """Get config value dengan default"""
        return self.config.get(key, default)

def main():
    loader = ConfigLoader("config.json")
    print(loader.get("host", "localhost"))  # first access: load file
    print(loader.get("port", 8080))
    
    # Second access: cached (no file read)
    print(loader.config)  # cached
    
    # Modify file outside, then reload
    loader.reload()
    print(loader.config)  # reloaded
```

---

## Referensi

- [Python Docs - Properties](https://docs.python.org/3/library/functions.html#property)
- [Real Python - Python Properties](https://realpython.com/python-property/)
- [functools.cached_property](https://docs.python.org/3/library/functools.html#functools.cached_property)
- [Python Data Model - Attribute Access](https://docs.python.org/3/reference/datamodel.html#customizing-attribute-access)

---

## Next Steps

Setelah menyelesaikan pelajaran ini, lanjutkan ke:
- [Class Methods, Static Methods & Special Methods](M07-L04)
